name: personalweb

services:
    backend:
        build: backend
        env_file:
            - backend/.env
        restart: on-failure
        ports:
            - "3000:3000"
    frontend:
        build: frontend
        ports:
            - "8080:5173"
